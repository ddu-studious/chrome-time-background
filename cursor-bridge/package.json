{
  "name": "cursor-bridge",
  "version": "0.1.0",
  "description": "Local bridge service exposing Cursor SDK agents via HTTP/SSE",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "postinstall": "node scripts/rebuild-native.js"
  },
  "dependencies": {
    "@cursor/sdk": "latest",
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
